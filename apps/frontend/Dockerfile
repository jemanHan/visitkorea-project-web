# apps/frontend/Dockerfile
FROM node:20-alpine

WORKDIR /app

# 패키지 파일 복사 및 의존성 설치
COPY package*.json ./
RUN npm ci

# 소스 코드 복사
COPY . .

EXPOSE 5173

# 개발 모드로 실행 (핫 리로드 지원)
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]
